# Day 01 — AWS Environment Setup & GitHub Configuration

## Objectives
- Install and configure AWS CLI
- Verify AWS credentials and default region
- Set up Git and GitHub repository for the challenge

## AWS CLI Setup
- Install AWS CLI and configure it for your account
- Verify CLI is working using:
```bash
aws --version
aws sts get-caller-identity
